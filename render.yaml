services:
  - type: web
    name: standgeld-standalone
    runtime: node
    plan: free
    buildCommand: npm install
    startCommand: npm start
    healthCheckPath: /api/health
    envVars:
      - key: NODE_VERSION
        value: 20
      - key: TZ
        value: Europe/Berlin
